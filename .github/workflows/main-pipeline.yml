name: CI/CD - Sistema de Ventas (Final Absoluto)

on:
  push:
    branches: ["main", "ci-dia4"]
  workflow_dispatch:

jobs:
  quality-gates:
    name: Quality Gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4" # Alineado con tus dependencias
          extensions: dom, curl, mbstring, pdo_sqlite, bcmath, intl, gd
      - name: Validate
        run: |
          composer install --no-ansi --no-interaction --prefer-dist
          rm -f tests/Feature/ExampleTest.php
          cp .env.example .env && php artisan key:generate
          mkdir -p database && touch database/database.sqlite
          ./vendor/bin/pint --test
          php artisan test --env=testing
        env:
          DB_CONNECTION: sqlite
          DB_DATABASE: database/database.sqlite

  build:
    name: Build Release
    needs: quality-gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4" # Crucial para que composer install no falle
      - name: Build
        run: |
          composer install --no-dev --optimize-autoloader
          npm ci && npm run build
          date +%Y%m%d_%H%M%S > RELEASE_ID
          tar -czf /tmp/release.tar.gz --exclude='./release.tar.gz' --exclude='./node_modules' .
          mv /tmp/release.tar.gz release.tar.gz
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: app-package
          path: release.tar.gz

  deploy-staging:
    name: Deploy to Staging
    needs: build
    runs-on: self-hosted
    environment: staging
    steps:
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: app-package

      # - name: Atomic Deploy & Health Verification
      #   run: |
      #     set -e
      #     BASE_PATH="/var/www/comprasventas"
      #     TIMESTAMP=$(date +%Y%m%d%H%M%S)
      #     RELEASE_PATH="$BASE_PATH/releases/stg-$TIMESTAMP"

      #     # 1. GOBERNANZA: Estructura y Backup
      #     mkdir -p "$BASE_PATH/shared/backups"
      #     source "$BASE_PATH/shared/.env"
      #     PGPASSWORD="$DB_PASSWORD" pg_dump -h "$DB_HOST" -U "$DB_USERNAME" -Fc "$DB_DATABASE" > "$BASE_PATH/shared/backups/pre-stg-$TIMESTAMP.sql" || echo "Backup skipped"

      #     # Asegurar que existan todas las subcarpetas necesarias en shared
      #     mkdir -p "$BASE_PATH/shared/storage"/{app,framework/views,framework/sessions,framework/cache,logs}
      #     mkdir -p "$RELEASE_PATH"
      #     tar -xzf release.tar.gz -C "$RELEASE_PATH"
          
      #     # 2. VINCULACIÓN
      #     ln -nfs "$BASE_PATH/shared/.env" "$RELEASE_PATH/.env"
      #     ln -nfs "$BASE_PATH/shared/storage" "$RELEASE_PATH/storage"
          
      #     # 3. PERMISOS INTEGRALES (Solución al Storage False)
      #     # Primero: shared/storage para escritura de datos
      #     sudo chown -R fellsing:www-data "$BASE_PATH/shared/storage"
      #     sudo chmod -R 775 "$BASE_PATH/shared/storage"
          
      #     # Segundo: El release actual (bootstrap/cache es vital)
      #     sudo chown -R fellsing:www-data "$RELEASE_PATH"
      #     sudo chmod -R 775 "$RELEASE_PATH/bootstrap/cache"
          
      #     # 4. ACTIVACIÓN DE MOTOR
      #     cd "$RELEASE_PATH"
      #     php8.4 artisan optimize:clear
      #     php8.4 artisan migrate --force
          
      #     # 5. SWITCH ATÓMICO
      #     ln -sfn "$RELEASE_PATH" "$BASE_PATH/current.new"
      #     mv -Tf "$BASE_PATH/current.new" "$BASE_PATH/current"
      #     sudo systemctl reload php8.4-fpm

      #     # 6. HEALTH CHECK DE CAJA BLANCA (Requisito 3.7)
      #     echo "Validando integridad del sistema..."
      #     sleep 5
      #     HEALTH_JSON=$(php8.4 artisan tinker --execute="echo json_encode(app(App\Http\Controllers\HealthController::class)->__invoke()->getData());")
          
      #     # Verificación estricta de la cadena de salud
      #     if [[ "$HEALTH_JSON" == *"\"status\":\"ok\""* && "$HEALTH_JSON" == *"\"storage\":{\"ok\":true}"* ]]; then
      #       echo "✅ SISTEMA ÓPTIMO: Salud verificada al 100%."
      #       cd "$BASE_PATH/releases" && ls -t | tail -n +6 | xargs -r rm -rf
      #     else
      #       echo "❌ FALLO DE SALUD: Estado degradado detectado."
      #       echo "Respuesta del sistema: $HEALTH_JSON"
      #       exit 1
      #     fi
      # 
      - name: Atomic Deploy & Application Health Gate
        run: |
          set -e
          BASE_PATH="/var/www/comprasventas"
          RELEASE_PATH="$BASE_PATH/releases/stg-$(date +%Y%m%d%H%M%S)"
          
          mkdir -p "$BASE_PATH/shared/storage"/{app,framework/views,framework/sessions,framework/cache,logs}
          mkdir -p "$RELEASE_PATH"
          tar -xzf release.tar.gz -C "$RELEASE_PATH"
          
          # Limpieza preventiva de caché hardcodeado
          rm -f "$RELEASE_PATH/bootstrap/cache"/*.php
          
          ln -nfs "$BASE_PATH/shared/.env" "$RELEASE_PATH/.env"
          ln -nfs "$BASE_PATH/shared/storage" "$RELEASE_PATH/storage"
          
          sudo chown -R fellsing:www-data "$BASE_PATH/shared/storage" "$RELEASE_PATH"
          sudo chmod -R 775 "$BASE_PATH/shared/storage" "$RELEASE_PATH/bootstrap/cache"
          
          cd "$RELEASE_PATH"
          php8.4 artisan optimize:clear
          php8.4 artisan view:clear
          php8.4 artisan migrate --force
          
          ln -sfn "$RELEASE_PATH" "$BASE_PATH/current.new"
          mv -Tf "$BASE_PATH/current.new" "$BASE_PATH/current"
          sudo systemctl restart php8.4-fpm
          
          sleep 5
          HEALTH_JSON=$(php8.4 artisan tinker --execute="echo json_encode(app(App\Http\Controllers\HealthController::class)->__invoke()->getData());")
          [[ "$HEALTH_JSON" == *"\"storage\":{\"ok\":true}"* ]] || exit 1

  deploy-production:
    name: Deploy to Production
    needs: deploy-staging
    if: github.ref == 'refs/heads/main'
    runs-on: self-hosted
    environment: Production
    steps:
      - name: Download
        uses: actions/download-artifact@v4
        with:
          name: app-package
      - name: Final Deploy
        run: |
          set -e
          TIMESTAMP=$(date +%Y%m%d%H%M%S)
          BASE_PATH="/var/www/comprasventas"
          RELEASE_PATH="$BASE_PATH/releases/prod-$TIMESTAMP"
          
          mkdir -p "$RELEASE_PATH"
          tar -xzf release.tar.gz -C "$RELEASE_PATH"
          ln -nfs "$BASE_PATH/shared/.env" "$RELEASE_PATH/.env"
          ln -nfs "$BASE_PATH/shared/storage" "$RELEASE_PATH/storage"
          sudo chown -R fellsing:www-data "$BASE_PATH/shared"
          sudo chmod -R 775 "$BASE_PATH/shared"
          
          cd "$RELEASE_PATH"
          php artisan optimize:clear
          php artisan migrate --force
          ln -sfn "$RELEASE_PATH" "$BASE_PATH/current"
          sudo systemctl reload php8.4-fpm
          
          sleep 10
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://192.168.100.63/api/health)
          [ $STATUS -eq 200 ] || exit 1

  # ==========================================
  # Tarea 5: Rollback Automático (Mitigación)
  # ==========================================
  rollback:
    name: Automatic Rollback
    needs: [deploy-staging, deploy-production]
    if: failure()
    runs-on: self-hosted
    steps:
      - name: Recover Last Stable Release
        run: |
          BASE_PATH="/var/www/comprasventas"
          PREVIOUS=$(ls -td $BASE_PATH/releases/stg-* | sed -n '2p')
          if [ -n "$PREVIOUS" ]; then
            ln -sfn "$PREVIOUS" "$BASE_PATH/current"
            sudo systemctl reload php8.4-fpm
            echo "🔙 Rollback ejecutado correctamente"
          fi